const Header = props => {
  return (
    <header className="bg-white text-gray-800 py-1 text-center">
      <nav>
        ARTISAN
        <br />
        FRONT
      </nav>
    </header>
  );
};

export default Header;
